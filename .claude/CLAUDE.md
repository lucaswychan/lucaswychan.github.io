# Project Instructions

## Code Formatting

After making any code changes, always run Prettier check before committing:

```bash
npx prettier . --check
```

To auto-fix formatting issues:

```bash
npx prettier . --write
```
